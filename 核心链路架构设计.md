# 核心链路架构设计

> 状态：**已定稿，待实施** · 日期：2026-08-04
> 本文是「收敛到核心链路」重构的蓝图文档。实施分三期（P1 后端链路 → P2 界面 → P3 清理），代码改动必须对照本文执行；实现与本文冲突时，先改本文再改代码。
> 配套文档：`功能模块与核心流程梳理.md`（现状全景）、`FEATURE_FLAGS.md`（B 端封存开关）

---

## 0. 已锁定的三个决策

| # | 决策 | 结论 | 直接后果 |
|---|------|------|---------|
| D1 | 审核闸门去留 | **移除，只留抽检+纠错** | `needs_review` 恒为 0，报告产出即家长可见；approve/reject/批量审核从主链路移除（P1 弃用、P3 删码）；质量控制全部由质量回路承担 |
| D2 | C 端入口 | **合并为单一家庭端** | `/parent`、`/s/<code>`、`/student` 三合一；四个上传入口收拢为一个；额度闸门只留一套 |
| D3 | 周报节奏 | **条件式自动出** | 每周六自动检查：本周有分析数据的学生自动出周报；无数据不出；运营端保留补发入口 |

---

## 1. 背景：为什么必须收敛

系统是沿需求顺序长出来的，价值链被埋在四类症状之下（均有代码佐证）：

1. **一条链路，四个上传入口，行为不一致**
   - `/api/upload`（app.py:6788，运营用）：只存文件不触发流水线
   - `/api/student/upload`（app.py:5466）：**无任何额度校验**，直接建任务
   - `/api/public/<code>/upload`（app.py:6338）：有额度校验，固定 stage=grade_only
   - `/api/parent/diagnose`（app.py:9714）：单文件、自动建档、无额度校验
2. **流程语义靠 stage 字符串传递**：weekly 有 7 个 stage 自由组合；`grade_only → analysis_only` 靠 worker 里的隐式自动链缝合（pipeline_worker.py `_process_task`）；轮询端点要为这条缝合链做专门的状态拼接（app.py:6403 `/api/public/<code>/task`）。
3. **两套流水线 80% 重复**：`onboarding_pipeline.py` 与 `weekly_pipeline.py` 的 full 段几乎同链（OCR→错题分析→薄弱矩阵→方案→报告），两份代码各自演化。
4. **主干已存在却没被当主干**：`weekly_records` 表就是周期状态机（paper_submitted / paper_analyzed / exercises_sent / exercises_completed / exercises_graded / report_sent），但只被当作顺手更新的标记位。

**收敛策略：不是重写，是把已有主干扶正。**

---

## 2. 核心链路定义（产品的唯一故事）

> 学生建档 → 入学诊断 → 每周拍照 → 自动批改分析 → 错题沉淀 → 针对性练习 → 周报看见成长 → 错题本越读越薄

```
              ┌────────────────────────────────────────────┐
              │           质量回路（贯穿，保护核心资产）       │
              │      抽检 → 纠错 → prompt 回流 → 成本护栏     │
              └────────────────────────────────────────────┘

  建档 ──→ 入学诊断(周期0) ──→ 周循环(周期1..N) ──→ 月度总结
  基本信息    首张照片            每周照片            每月1日自动
  画像        │                   │
  订阅        ▼                   ▼
          同一条流水线 ──────→ 【错题本】核心资产
                                │        ↑
                    ┌───────────┤        │ 日常在线练习
                    ▼           ▼        │ 连对2次=掌握
              薄弱矩阵+方案   针对性练习15题─┘
                    │
                    ▼
              分析报告 → 周报(周六条件自动) → 错题本快照(周一) → 月度总结(1日)
```

**核心资产论**：错题本（学生薄弱点图谱）是系统唯一的累积性资产。所有 AI 能力要么是它的输入（诊断、批改），要么是它的输出（练习、报告、快照、总结）。功能分两类：

- **主链路**：建档、诊断、周循环、练习、周报、错题本、月度总结、质量回路
- **增强层**（明确挂载，不进入主链路叙事）：成就墙/打卡/复盘/转介绍=留存层；授权/删除=合规层；预算/告警/审计/备份=运维层；学校/班级=B 端封存层

---

## 3. 领域模型：Cycle（学习周期）状态机

### 3.1 定义

`Cycle` 是一等公民：**一个学生 × 一个周期起点 × 一种类型 = 一个 Cycle 实例**。所有 AI 任务必须附着在一个 Cycle 上，推进它的状态；不再有孤儿任务。

```
Cycle 身份:  (student_id, week_start, kind)
  kind = 'diagnostic'   入学诊断（周期 0，每学生至多一个）
  kind = 'weekly'       周循环（每周一）

状态机（线性推进，只进不退；advance 对回退/重复幂等）:

  created ──收到照片──→ paper_received
           ──OCR 完成──→ ocr_done
           ──错题入库──→ graded          ← 错题本资产更新点
           ──矩阵+方案──→ analyzed
           ──分析报告产出──→ report_ready
           ──练习就绪──→ exercised
           ──周报产出──→ reported        ← 周循环终态
```

- diagnostic 周期：created → paper_received → ocr_done → graded → analyzed → report_ready（诊断报告即终态产物，无练习/周报节点）
- 练习作答与掌握度收敛发生在周期之外（日常行为），通过 `practice_records` 直接更新错题本，不改 Cycle 状态

### 3.2 DB 落地（最小侵入，扩展现有表）

`weekly_records` 扶正为 Cycle 表（**不新建表、不改名**，保留兼容）：

```sql
ALTER TABLE weekly_records ADD COLUMN kind TEXT DEFAULT 'weekly';      -- diagnostic | weekly
ALTER TABLE weekly_records ADD COLUMN stage TEXT DEFAULT 'created';    -- 状态机当前态
ALTER TABLE weekly_records ADD COLUMN updated_at TIMESTAMP;
-- 现有 6 个布尔标记保留为兼容视图，P3 评估移除；
-- UNIQUE(student_id, week_start) 扩展为 UNIQUE(student_id, week_start, kind)
```

`ai_tasks` 增加关联：

```sql
ALTER TABLE ai_tasks ADD COLUMN cycle_id INTEGER REFERENCES weekly_records(id);
-- 回填：按 (student_id, week_start) 匹配现有 weekly_records
```

### 3.3 运营端视角

运营端主界面从「20 个平铺 tab」改为链路导航：**学生 → 当前周期 → 状态机各环节**。任一环节卡住（如 graded 停留超 48h）即为待办。

---

## 4. 流水线：一条声明式链，多个恢复点

### 4.1 链定义（取消 stage 自由组合）

```python
# 分析主链（P1 实现定稿：analysis_report 在 exercises 之前，与现状进度叙事一致）
WEEKLY_CHAIN     = ["ocr", "analyze", "plan", "analysis_report", "exercises"]
DIAGNOSTIC_CHAIN = ["ocr", "analyze", "plan", "analysis_report"]
# weekly_report 为独立节点（不在主链上）：手动补发 / 周六条件自动共用
```

| 节点 | 职责 | 产物 | 状态推进 |
|------|------|------|---------|
| `ocr` | 多页并行 OCR | ocr_text（持久化到 input_data._ocr_text） | → ocr_done |
| `analyze` | 错题分析+归一化过滤 | 错题入库+session | → graded |
| `plan` | 薄弱矩阵+方案（已有→增量更新/无→新建） | learning_plans 更新 | → analyzed |
| `analysis_report` | 矩阵分析/诊断报告 | report_pdf HTML 文件 | → report_ready |
| `exercises` | 题库复用优先+LLM 补齐（每错题 2 题，无总量上限） | questions 入库+练习卷 HTML | → exercised |
| `weekly_report` | 家长周报（独立节点） | weekly_pdf HTML 文件 | → reported |

diagnostic 链 = `DIAGNOSTIC_CHAIN`（无 exercises、无 weekly_report）。

### 4.2 恢复点映射（对外 API 契约不变）

`POST /api/pipeline/run` 的 `stage` 参数**语义不变**，内部映射为链起点：

| 现有 stage 参数 | 链执行区间 | 说明 |
|----------------|-----------|------|
| `full` / onboarding | `ocr → exercises` / diagnostic 链 | 完整链 |
| `grade_only` | `ocr → exercises` | 批改试卷按钮；**一次任务跑完，不再自动链第二个任务** |
| `analysis_only` | `plan → exercises` | 矩阵分析按钮 |
| `report_only` | `weekly_report` 单节点 | 周报补发按钮 / 周六自动 |
| `grade_exercises` | 独立子链（答案批改，不挂主链） | 保留现状，挂当前 Cycle |
| `weekly_mistake_book` / `monthly_summary` | 独立快照任务 | 保留现状 |

### 4.3 引擎执行模型

`pipeline/engine.py`（P1 已实现）：

```
run_chain(task, db_path, kind, start_param):
  cycle = get_or_create_cycle(student, week, kind)   # 任务必须挂周期
  attach_task(task_id, cycle.id)
  断点续跑决策:
    僵尸复活(_auto_resumed 标记)且链已完成 → 按 files 表重建输出，不重复执行
    否则按 cycle.stage 跳到下一未完成节点（analyze 需要 _ocr_text，缺失则退回恢复点）
  for node in CHAIN[从起点:]:
      NODE_FUNCS[node](ctx)                          # 节点是纯函数
      advance_cycle(cycle, NODE_DONE_STATE[node])    # 每节点落库一次状态
  return ctx.build_output()
```

- **断点续跑**：僵尸任务复活时读 `cycle.stage`，从下一节点继续；OCR 文本从 `input_data._ocr_text` 恢复，免重复识别。仅带 `_auto_resumed` 标记的复活任务才走重建；用户主动重跑（新试卷/再次矩阵分析）照常执行
- **进度映射**：节点内部沿用现有 progress 文案；缝合链拼接逻辑已从 `/api/public/<code>/task` 删除
- **额度**：链起点为 `ocr` 的运行消耗 1 额度；`plan`/`weekly_report` 起点免费；失败退额度（quota_charged）保留，`/api/pipeline/run` 现在也写入 quota_charged（此前运营端触发无退还标记）
- **质量 hook**：任务完成后由 worker 随机抽检（现状保留）；成本预算熔断保留在 `/api/pipeline/run` 前置
- **onboarding 与 weekly 已合并**为 `pipeline/cycle_pipeline.py`（kind 参数化），两个旧文件已删除

### 4.4 审核闸门移除（D1）

- `get_plan_needs_review` 恒返回 False，`needs_review` 写入恒 0
- `approve/reject/batch` 端点 P1 标记弃用（返回 410），P3 删除
- 驳回重跑机制（teacher_notes 回灌）删除——纠错回流替代其职能

---

## 5. 入口收敛：单一家庭端（D2）

### 5.1 形态

`/s/<code>` 成为唯一 C 端界面，两种模式：

| 模式 | 触发 | 内容 |
|------|------|------|
| **首访模式** | 无 code（`/parent` 重定向至此） | 学情体检：拍照→建档→诊断→获得 code，localStorage 保存 |
| **学习中心** | 有 code | 现有 9 tab 全量功能 |

- `/parent` 保留 URL（对外投放过），内容重定向/改造为首访模式容器
- `/student-login` 保留，登录成功后跳转该学生的 `/s/<code>`（code 从档案读取）；`/student` 页 P3 删除
- 学生注册（`/register`）保留为获得 code 的方式之一，注册成功同样进 `/s/<code>`

### 5.2 统一上传与额度闸门

四个入口收拢为 `POST /api/family/upload`（code 作用域）：

```
统一闸门顺序: 订阅 active → 额度检查 → consume_quota(quota_charged) → 存文件 → 建任务(挂 Cycle)
```

- 修复现状 `/api/student/upload` 的额度漏洞（并入统一闸门后自然消失）
- 首访诊断（bootstrap）走 trial 额度，同一闸门
- 运营端上传（`/api/upload` + `/api/pipeline/run`）保留为独立路径（staff 豁免额度），但文件登记与任务创建复用同一 domain 函数
- `domain/quota.py`（新）收拢 check/consume/refund/reset，全系统唯一调用点

---

## 6. 周报条件式自动出（D3）

`pipeline_worker._scheduler_loop` 增加周六分支：

```
每周六 08:00 后（现有周一/1号分支旁）:
  for student in 活跃学生:
      cycle = 本周 Cycle
      if cycle.stage >= graded 且本周有新错题或分析数据:
          创建 weekly_report 任务（DB 级当日去重，沿用 _trigger_batch_tasks 模式）
      else:
          不出（避免空报）；运营端链路视图提示「本周未上传」
```

- 手动补发 = 运营端「生成周报」按钮（report_only 恢复点），两条路径共用同一节点，DB 去重防双发
- 周报内容逻辑（render_weekly_report 及三方行动方案装配）不变

---

## 7. 质量回路（主链路的一部分，不是外挂）

```
每个 stage 出口:
  ├─ 成本检查（check_cost_budget，超熔断中止并告警）—— 现状前置，保留
  └─ 抽检采样（完成后随机 2 条入 aigc_safety_checks）—— 现状保留

老师纠错（保留并强化）:
  ai_corrections → ai_feedback_patterns → prompt 自动注入（现状已通）
  运营端质量视图: 待抽检队列 + 纠错记录 + 纠错命中率统计
```

移除审核闸门后，质量回路是唯一的 AI 输出控制面。运营老师（你）的日常工作面 = 抽检队列 + 纠错，而非全量审核。

---

## 8. 目标模块结构与现状映射

```
app.py                     # 仅装配: Flask 实例/注册蓝图/启动 worker（目标 <300 行）
web/
  family_app.py            # 家庭端页面（现 STUDENT_PAGE + PARENT_PAGE 合并）
  ops_console.py           # 运营端页面（现主后台模板迁出）
api/
  family_api.py            # 家庭端 API: bootstrap/upload/practice/reports/progress
  ops_api.py               # 运营 API: 学生/周期/质量/成本/合规/题库
domain/                    # 领域服务，不碰 HTTP，可单测
  cycle.py                 # ★ Cycle 状态机（advance/attach/查询）
  mistakes.py              # 错题本资产
  questions.py             # 题库（收拢现 3 处重复取题逻辑:
                           #   api_public_practice / api_public_exercise_pdf / grade_exercises 回退）
  reports.py               # 报告装配（调 report_templates）
  quota.py                 # 订阅额度唯一闸门
  quality.py               # 抽检/纠错
pipeline/
  engine.py                # ★ 声明式链执行器（断点续跑/质量 hook）
  cycle_pipeline.py        # onboarding+weekly 合并体
  stages.py                # ocr/analyze/plan/exercises/analysis_report/weekly_report
  scheduler.py             # 周六周报/周一错题本/月度总结（现 _scheduler_loop 迁出）
infra/                     # 基础设施
  db.py                    # 数据访问（现状基本不动，加迁移）
  llm.py                   # LLM 抽象（不动）
  ocr.py                   # OCR（skills_bridge 的 OCR 部分迁此）
  backup.py                # 不动
skills_bridge.py           # P1 末拆空: prompts+业务函数 → pipeline/stages.py
report_templates.py        # 不动（渲染层已独立）
```

---

## 9. 实施计划

### P1 — 收敛后端链路（✅ 2026-08-04 完成，不动界面，API 契约兼容）

| # | 任务 | 涉及文件 | 验收 | 状态 |
|---|------|---------|------|------|
| 1 | weekly_records 加 kind/stage 列 + 唯一约束迁移；ai_tasks 加 cycle_id 并回填 | db.py | 迁移幂等，旧数据可读 | ✅ 副本演练通过（stage 派生 + cycle_id 回填 24/24 + 幂等 + 唯一约束） |
| 2 | domain/cycle.py 状态机 | 新文件 | 单测覆盖全部状态迁移 | ✅ tests/test_cycle.py 8 用例 |
| 3 | pipeline/engine.py 声明式链 + 恢复点 + 断点续跑 | 新文件 | 从各恢复点启动结果与现状一致 | ✅ tests/test_cycle_pipeline.py 覆盖 |
| 4 | 合并 onboarding/weekly → cycle_pipeline.py；删两个旧文件 | pipeline/ | tests/ 全绿 + demo 模式冒烟 | ✅ |
| 5 | 删自动链 hack（_process_task 缝合段）；简化 /api/public/<code>/task 轮询 | pipeline_worker.py, app.py | grade_only 一次任务出练习+报告、无第二任务 | ✅ worker 路径冒烟用例 |
| 6 | 审核闸门弃用：needs_review 恒 0，approve/reject 返回 410 | app.py, db.py | 报告产出即公开页可见 | ✅ |
| 7 | 调度器加周六条件式周报 | pipeline/scheduler.py | 有数据学生出报、无数据不出、当日去重 | ✅（顺带修复：去重改用 date('now') UTC 自洽，原 Python 本地日期与 UTC created_at 混用） |
| 8 | domain/quota.py 统一闸门；修复 /api/student/upload 额度漏洞 | app.py, 新文件 | 四入口共用同一闸门 | ✅（顺带修复：create_student/update_student 订阅按 PRICING 回填额度 + _ensure_quota_reset 兜底） |

**P1 完成标志**（已达成）：`domain/cycle.py` + `pipeline/` 能完整讲出产品故事；`/api/pipeline/run` 现有调用方（运营端前端）零改动。

### P2 — 收敛界面（✅ 2026-08-04 完成）

| # | 任务 | 状态 |
|---|------|------|
| 9 | 家庭端合并：/parent 首访模式 + 回访跳 /s/<code>；/student 弃用为跳转；注册/登录后统一进 /s/<code> | ✅（含 4 个验收用例） |
| 10 | 统一上传：存文件+闸门+触发收拢为 domain/upload.py，三个触发端点统一调用 | ✅（设计微调：不新增 /api/family/upload URL，避免冗余表面，见实施记录） |
| 11 | 运营端链路视角：仪表盘/周度列表显示 Cycle 状态机当前态 + 中间态停留 48h「卡住」告警 | ✅（stage_label/is_stuck，含 2 个单测） |
| 12 | app.py 拆蓝图（web/ + api/），主文件降至装配层 | ✅（9949 行 → 237 行；模板同步拆出） |

### P3 — 清理固化（✅ 2026-08-04 完成）

| # | 任务 | 状态 |
|---|------|------|
| 13 | 删除审核队列代码（approve/reject/batch 端点、review_queue 统计、前端审核区块、needs_review 字段链）；/student 页与 session 态学生端点删除 | ✅（逐条纠错入口迁至「周度服务→历史任务」操作列） |
| 14 | 取题逻辑收拢 domain/questions.py（3 处重复 → 1 个服务，unmastered_only/with_fallback 参数化） | ✅（4 个单测） |
| 15 | B 端代码隔离到 b_end/ 蓝图（18 条路由）；根目录杂物归档 archive/ 与 scripts/archive/ | ✅ |
| 16 | 修复 db.py `_migrate_db` 重复定义（删除被遮蔽的陈旧第一定义，price 列迁移并入生效定义）；一次性脚本归档 | ✅ |
| 17 | README / DEVELOPMENT.md 重写为新架构模型 | ✅ |

---

## 10. 兼容与风险

| 风险 | 缓解 |
|------|------|
| 运营端前端依赖 stage 语义 | API 契约不变（stage 参数映射为恢复点），前端零改动 |
| 迁移回填出错 | 全部迁移幂等 + 先备份（backup.py 手动触发）后迁移；data.db.bak_clean 已有先例 |
| 断点续跑引入状态不一致 | stage 纯函数幂等设计；节点产物以文件+DB 双落，重跑覆盖 |
| 周报自动化误发 | 条件检查 + DB 当日去重双保险；首周人工核对 |
| 家长轮询行为变化 | 缝合链消失后，grade_only 任务时长变长（含分析段），进度条按新链节点映射，前端文案同步 |
| 单 worker 锁语义 | 按学生锁保留；链执行期间同学生新任务排队，与现状一致 |

## 11. 遗留问题（不在本次范围）

- 报告 PDF 化（现状仅练习卷有 PDF）
- sms.py 接真实短信服务商（上线家长手机号体系前必须）
- app.py 前端模板是否进一步迁出为独立静态文件（P2 后评估）
- 转介绍/成就墙等留存层与主链路的挂载点规范化（增强层 API 冻结，不再扩张）

---

## 12. P1 实施记录（2026-08-04）

### 新增/变更文件

| 文件 | 说明 |
|------|------|
| `domain/__init__.py` `domain/cycle.py` | 新增：Cycle 状态机（8 状态，只进不退，advance 幂等） |
| `domain/quota.py` | 新增：订阅+额度统一闸门 charge_analysis（staff 豁免） |
| `pipeline/__init__.py` `engine.py` `stages.py` `snapshots.py` `cycle_pipeline.py` `scheduler.py` | 新增：声明式链执行器、6 链节点、3 快照任务、统一 handler、调度器（含周六条件周报） |
| `db.py` | 迁移：weekly_records→Cycle 表（kind/stage/updated_at + UNIQUE(student,week,kind) 重建表）；ai_tasks.cycle_id 回填；订阅额度回填修复；get_plan_needs_review 弃用恒 False |
| `pipeline_worker.py` | 删自动链 hack；调度器迁出；保留僵尸恢复/退额度/抽检 |
| `app.py` | pipeline 注册切换；四上传入口接统一闸门；审核端点 410；公开页轮询简化 |
| `onboarding_pipeline.py` `weekly_pipeline.py` | **已删除**（逻辑迁入 pipeline/） |
| `tests/test_cycle.py` `tests/test_cycle_pipeline.py` | 新增：状态机 8 用例 + 链路验收 9 用例（demo 模式） |

### 实现中的设计决议（与原稿差异，已回写 §3/§4）

1. **状态机 8 态**：插入 `report_ready`（分析报告产出后），消除断点续跑歧义
2. **链序**：`ocr → analyze → plan → analysis_report → exercises`（analysis_report 在 exercises 前，与现有进度叙事一致）；`weekly_report` 为独立节点，full/grade_only **不**自动出周报（周报唯一自动化途径 = 周六条件触发，唯一手动途径 = report_only 补发）
3. **重建语义**：仅 `_auto_resumed` 的僵尸复活任务在链已完成时重建输出；用户主动重跑照常执行（新试卷错题照常入库）
4. **方案更新策略统一**：已有方案 → 增量更新（generate_plan_update），无方案 → 新建。原 full 分支「每次从头重建方案」改为增量（与 analysis_only 对齐，属有意改进）
5. **空练习卷不再产出**：无题目时 exercise_file_id 为 None（原 full 分支产出空卷）

### 上线注意（首次启动）

- `python app.py` 启动时 init_db 自动执行迁移（重建 weekly_records + 回填），**先确认 data.db.bak_p1 备份在**；迁移幂等，可重复启动
- 现有 2 条 weekly_records 会被派生为 stage=exercised/reported（按布尔标记推导），24 条 ai_tasks 回填 cycle_id
- 审核队列界面仍会显示（空），属预期——P3 删除；smoke_test_fixes.py 引用已删模块，P3 归档前勿运行

---

## 13. P2 实施记录（2026-08-04）

### 新增/变更文件

| 文件 | 说明 |
|------|------|
| `domain/upload.py` | 新增：统一上传服务 family_upload（存文件 → 额度闸门 → 建任务入队，单一实现） |
| `domain/cycle.py` | 扩展：stage_label / is_stuck（链路状态展示 + 卡住判定，48h 阈值） |
| `web/shared.py` | 新增：装饰器 + 共享辅助 + UPLOAD_DIR/VERSION（自 app.py 拆出） |
| `web/pages.py` | 新增：页面蓝图（10 条页面路由） |
| `web/templates_admin.py` `templates_auth.py` `templates_family.py` | 新增：内联模板拆出（原 app.py 内 6 个大模板） |
| `api/family_api.py` | 新增：家庭端 API 蓝图（29 条路由） |
| `api/ops_api.py` | 新增：运营端 API 蓝图（89 条路由） |
| `app.py` | **9949 行 → 237 行**：仅装配（Flask 实例 + 蓝图注册 + 流水线注册 + 备份调度 + CLI） |
| `db.py` | dashboard 周状态 JOIN 加 kind='weekly'（防 diagnostic 周期重复行）+ stage/updated_at 输出 |
| 删除 | `STUDENT_DASHBOARD_PAGE` 孤儿模板（无任何渲染引用） |
| `tests/test_family_entry.py` `tests/test_pages_smoke.py` | 新增：家庭端 4 用例 + 页面冒烟 7 用例 |

### 蓝图拆分方法与安全网

- AST 驱动脚本确定性切片（函数按行号范围原样迁移，仅重写 `@app.route` → `@<bp>.route`），零手抄；拆分前备份 `app.py.bak_p2`
- 拆分前先建立页面冒烟基线测试（7 用例），拆分后全绿
- 拆分后 pyflakes 全量扫描修复未定义/未导入名称；路由注册数 129 条与拆分前一致
- 路由路径零变化 → 前端 JS 零改动

### 实现中的设计决议（与原稿差异）

1. **不新增 `/api/family/upload` URL**：统一上传以 `domain/upload.py` 服务函数收口，现有三个端点（public upload / student upload / parent diagnose）统一调用。URL 表面不扩张，收敛目标同样达成
2. **`/parent` 即首访模式**：直接渲染学情体检页（PARENT_PAGE，此前是孤儿模板无路由渲染），回访者由页面 JS 跳 `/s/<code>`——比「重定向容器」少一跳
3. **运营端链路视角取轻量形态**：仪表盘待处理表与 `/api/weekly` 增加链路状态列（状态徽章 + 卡住标红），未做完整导航重构；「学生 → 周期 → 环节」深度链路视图留待有运营反馈后再评估
4. **`/api/student/upload` 保留为登录态端点**：内部已走统一服务，P3 随 /student 页一并评估删除

### 验证结果

- pytest：**51 passed**（拆分前 44 + 页面冒烟 7）
- 启动冒烟：DB 副本 init_db + 蓝图注册 129 规则 OK；CLI list-admins OK

---

## 14. P3 实施记录（2026-08-04）

### 删除清单（P3-13）

- 审核端点 4 条：`/api/tasks/<id>/approve|reject`、`/api/tasks/batch/approve|reject`（P1 起已 410，P3 删码）
- `db.get_dashboard_stats` 的 review_queue 构建；`get_teacher_workload_stats` 审核口径统计（保留「本周待上传试卷」）；`get_operations_stats.pending_review`
- `db.mark_task_done` 的 needs_review 参数（恒写 0）；`db.get_plan_needs_review` 删除；PRICING 的 needs_review 配置删除
- 前端：概览页审核区块（HTML + approve/reject/batch JS + review-badge CSS）
- `/student` 页与 4 个 session 态端点（/api/student/me|reports|upload|progress）、`student_required` 装饰器
- **纠错入口迁移**：逐条纠错按钮从审核队列迁至「周度服务 → 历史任务」操作列（P1 后审核队列恒空，纠错实际已不可达，此为功能恢复）

### 新增/变更（P3-14~17）

| 文件 | 说明 |
|------|------|
| `domain/questions.py` | 新增：取题服务 get_practice_questions（主路径保留 source 关联；兜底切断关联防跨学生污染；unmastered_only/with_fallback 参数化） |
| `api/family_api.py` `pipeline/snapshots.py` | 3 处取题重复逻辑改为调用统一服务；练习卷 PDF 获得题库兜底（原 404） |
| `b_end/__init__.py` `b_end/routes.py` | 新增：B 端封存蓝图（18 条 flag 守卫路由，AST 脚本自 ops_api/pages 迁移） |
| `db.py` | 删除被遮蔽的第一个 `_migrate_db`；price 列防御性迁移并入生效定义 |
| `README.md` `DEVELOPMENT.md` | 重写：新架构、新流程、新数据模型（35 表）、测试约定、重构历史 |
| `FEATURE_FLAGS.md` | 更新：B 端代码位置指向 b_end/routes.py |
| 归档 | 根目录杂物 → `archive/`（测试产物/日志/未引用 traineddata/备份）；一次性脚本 → `scripts/archive/` |
| `tests/test_questions.py` | 新增：取题服务 4 用例 |

### 实现中的设计决议

1. **练习卷 PDF 启用题库兜底**：原实现无关联题时 404，统一后与练习 tab 行为一致（按知识点匹配题库题）
2. **批改任务取题 unmastered_only=False**：批改需覆盖已发练习卷全集（含在线已练对的题），与在线练习取题口径区分
3. **traineddata 归档**：根目录未压缩语言包未被引用（OCR 走 tessdata/ 内 .gz），移入 archive/ 而非删除
4. **移植代码的轻微 pyflakes 警告保留**（未用局部变量等）：P3 原则为最小 diff，不做装饰性修改

### 验证结果

- pytest：**55 passed**（51 + 取题 4）
- pyflakes 全量扫描：无未定义名称（剩余为移植代码自带轻微警告）
- 启动冒烟：DB 副本 init_db + 120 规则注册 + b_end 蓝图 OK；CLI list-admins OK
- 路由数核对：129（P2 拆分后）− 9（P3-13 删除：4 审核 + 4 学生端点 + /student 页）= 120 ✓
