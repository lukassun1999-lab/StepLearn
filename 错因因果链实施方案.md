# 错因因果链 · 实施方案

> 差异化支柱 A（真实数据）× 支柱 C（英语纵深）的核心落点：
> 把诊断从"哪里不会"（知识点统计）升级为"**为什么不会、先补什么**"（错因因果链）。
> 竞品告诉你"哪里不会"（横断面）；我们告诉你"核心矛盾是什么、哪些错题是次生表现、先补哪个根因"（因果链）——这是资深英语老师的阅卷思维，产品化。

---

## 0. 目标

每份诊断输出三个层次的结论：
1. **家长一句话**（模板填充）："孩子这周真正卡住的是词汇量——语法错题里 3 道是被生词绊倒的，先补词汇，语法问题会自己松动"
2. **错因结构画像**：五类错因分布 + 核心瓶颈（primary cause）+ 因果链
3. **根因优先的聚焦建议**："本周聚焦 2-3 个知识点"按因果链取根因，不是按错误率取最高

## 1. 设计原则

- **受控分类**：错因 = 五类枚举（词汇/语法/句法/语篇/审题），不是自由文本 → 可统计、可追踪、可校准、可跨周对比
- **因果链是"诊断"不是"生成"**：LLM 只做归因和判断；家长话术走模板填充 → 符合 AI 内容管控原则（凯哥红线）
- **渐进落地**：先本周画像，再跨周轨迹；不回溯历史数据（存量错题无错因，回溯只会不准确）

## 2. 数据结构变更

### mistakes 表新增 2 列（迁移，复用 db.py 现有的 PRAGMA 检查 + ALTER 模式）
| 列 | 类型 | 说明 |
|---|---|---|
| `error_cause` | TEXT | 五类枚举：vocab / grammar / syntax / discourse / careless |
| `cause_evidence` | TEXT | 判断依据（如"答案与正确词形相似但时态错误，题干含生词 XXX"） |

### 新表 `cause_profiles`（每学生一条，upsert）
```sql
CREATE TABLE IF NOT EXISTS cause_profiles (
    student_id INTEGER PRIMARY KEY,
    primary_cause TEXT,               -- 核心瓶颈（五类之一）
    primary_evidence TEXT,            -- 判断证据
    cause_chain TEXT DEFAULT '[]',    -- JSON: [{"from":"词汇","to":"语法","note":"..."}]
    secondary_causes TEXT DEFAULT '[]', -- JSON: 次生错因列表
    priority_kps TEXT DEFAULT '[]',   -- JSON: 按因果链聚焦的知识点（3个）
    plain_language TEXT,              -- 给家长的一句话（模板填充）
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```
- init_db 建表 + 迁移循环加列（0.5 天）

## 3. AI 分析流程（两段式）

### 段 1：analyze_mistakes 增强（现有 prompt 小改，0.5 天）
`MISTAKE_ANALYSIS_PROMPT` 中错误分析字段从自由文本 `error_reason` 改为受控结构：

```
"error_cause": "vocab | grammar | syntax | discourse | careless",
"cause_evidence": "一句话证据"
```

- 五类定义 + 每类 2-3 个典型例子写进 prompt（稳定分类的关键）
- 兜底：LLM 输出缺 `error_cause` 时，用 `error_reason` 关键词映射（拼写/单词→vocab；时态/语态/介词→grammar；长难句→syntax；主旨/推理→discourse；粗心/漏看→careless），仍缺省默认 grammar
- `add_mistake()` 增加 `error_cause`、`cause_evidence` 两个参数入库

### 段 2：新函数 analyze_cause_chain(student_id, mistakes_json, task_id)（0.5 天）
输入：本次错题（question_type / question_text / error_cause / knowledge_points）+ 近 4 周错因统计
输出（结构化 JSON，存 cause_profiles）：

```
{
  "primary_cause": "vocab",
  "primary_evidence": "本周 8 道错题中 5 道含未掌握词汇；阅读错题全部伴随生词",
  "cause_chain": [
    {"from": "词汇", "to": "语法", "note": "词汇不足导致无法判断句子成分"},
    {"from": "词汇", "to": "语篇", "note": "生词阻断句意连贯"}
  ],
  "secondary_causes": ["语法", "审题"],
  "priority_kps": ["高考高频词汇", "现在完成时（受词汇连累，次生）"],
  "plain_language": "孩子这周真正卡住的是{词汇量}——{语法错题里3道是被生词绊倒的}，先补{高频词汇}，{语法问题会自己松动}。"
}
```

- `plain_language` 用模板填空，LLM 只填数据不自由发挥话术（内容管控）
- 放置：在 `node_plan` 内部开头调用（不新增 pipeline 节点，断点续跑逻辑不受影响；断点重建诊断时也能跑）

### 聚焦逻辑改造（0.5 天）
- `_build_diagnosis()` 输出增加 `cause_profile` 字段（从 cause_profiles 读）
- `LEARNING_PLAN_PROMPT`：诊断输入附带因果链画像；`weak_point_priority[].reason` 要求引用因果链（"词汇是核心瓶颈，语法错题为次生"）
- 报告"本周聚焦"优先取 `priority_kps`（根因）而非错误率最高

## 4. 报告呈现（0.5 天，report_templates.py + templates_family.py）

诊断报告/周报新增"**错因画像**"板块：

1. 一句话（plain_language，运营 review 时可改）
2. 五类错因分布（横向条形，颜色区分）——家长侧显示为通俗名：

| 内部枚举 | 家长侧文案 |
|---|---|
| vocab | 单词不认识 |
| grammar | 语法规则没掌握 |
| syntax | 长句拆不开 |
| discourse | 读不懂文章逻辑 |
| careless | 看题不仔细 |

3. 因果链可视化（CSS/SVG 箭头：词汇 → 语法 / 语篇）
4. "本周聚焦"改为根因优先，标注"先补根因，次生问题会自己松动"

## 5. 分阶段落地

### Phase 1 — 本周画像（核心闭环） ✅ 已完成（2026-08-05）
db 迁移 + prompt 改造 + analyze_cause_chain + node_plan 接入 + 报告板块 + 测试
**追加实现**：方案生成引用因果链（LEARNING_PLAN_PROMPT 规则 + `_reorder_priority_by_cause` 确定性重排）、运营校准修正（见 §8）

### Phase 2 — 跨周轨迹 ✅ 已完成（2026-08-05）
- 新表 cause_profile_history（student_id + week_start 唯一，同周覆盖）
- `build_cause_trend()`：模板叙事（同卡点缓解/加剧/持平、卡点迁移"补上来了"），非 LLM 生成
- 周报新增"🧭 卡点变化"板块：叙事 + 上周/本周卡点占比对比
- 写入时机：node_plan 分析链路落库；周报（report_only）只读不重算，与三按钮机制兼容

### Phase 3 — 错因 × 知识点双维矩阵 ⏸️ 暂缓
- 聚合"词汇×哪些知识点、语法×哪些知识点"，练习生成按根因知识点优先
- **建议先用真实诊断跑 2-3 周**积累画像数据、验证叙事准确性后再启动

## 6. 验证与校准

- **运营校准**：你以运营老师身份人工 review 10 份真实诊断，核对 error_cause 分类准确率；错分超过 2 成 → 调整 prompt 的类别定义和例子
- **自动化测试**：demo 模式单测（cause_chain 结构校验、plain_language 模板完整性、报告渲染含新板块、兜底映射）
- **间接指标**：家长侧"聚焦建议"对应练习的完成率，作为因果链质量的下游信号

## 7. 风险与边界

| 风险 | 对策 |
|---|---|
| LLM 分类不稳 | 五类定义+例子受控输出；关键词兜底映射；运营 QC 终审 |
| 单周样本少 | 因果链聚合"近 4 周 + 本次"；样本不足时降级为"基于本次试卷" |
| 存量数据无错因 | 不回溯，画像从上线起积累 |
| 家长话术失准 | plain_language 模板化 + 运营可改，LLM 不自由发挥 |

---

## 8. 实施记录（2026-08-05）

### 实际改动文件
- **db.py**：mistakes 加 `error_cause`/`cause_evidence` 列（自动迁移）；新表 `cause_profiles`、`cause_profile_history`；`add_mistake` 扩展；`save/get_cause_profile`、`save/get_cause_profile_history`
- **skills_bridge.py**：`MISTAKE_ANALYSIS_PROMPT` 规则 6/7（未作答不进 mistakes、受控五类+证据）；`CAUSE_CHAIN_PROMPT`；`LEARNING_PLAN_PROMPT` 因果链规则段；`_normalize_error_cause`（题型加权+关键词兜底，未作答返回 None）；`_statistical_cause_profile`（LLM 不可用兜底）；`analyze_cause_chain`；`build_cause_trend`
- **pipeline/stages.py**：`node_analyze` 传错因；`node_plan` 生成画像+写周历史（失败不阻断）；`_reorder_priority_by_cause`（根因优先确定性重排）；`_build_diagnosis` 带画像；`node_analysis_report`/`node_weekly_report` 传画像与对比
- **report_templates.py**：诊断报告"🔍 错因画像"板块（卡点+家长一句话+因果链+聚焦标签）；周报"🧭 卡点变化"板块
- **生产库（data.db）**：已完成迁移（加列、cause_profiles、cause_profile_history）

### 与方案的差异（实际实现）
1. `analyze_cause_chain` 签名按 `(student, mistakes, task_id)` 实现，近期错题在函数内从 DB 聚合
2. **统计兜底画像**（`_statistical_cause_profile`）：方案未提，实际作为 LLM 不可用时的保底（demo 模式/API 失败都走它），保证主链路稳定
3. **方案重排**（`_reorder_priority_by_cause`）：prompt 引导之外加了确定性保障，根因知识点按 priority_kps 顺序排最前、severity 升"高"
4. `plain_language` 兜底模板在统计画像中也生成（不依赖 LLM）
5. demo 模式 LLM 返回 `{}`（llm.py DEMO_DATA 无 cause_chain）→ 自然走统计兜底，测试稳定

### 运营校准（第一轮，详见《错因校准报告.md》）
- 样本：生产库 83 条真实错题（存量旧数据，用于校准兜底映射）
- 发现并修正 3 个系统性问题：
  1. **未作答污染统计**（~20 条"学生未作答"被误归错因）→ `_is_unanswered` 识别跳过 + prompt 规则 6 补充
  2. **题型特征未利用**（完形词义辨析误归语篇、情景交际默认语法）→ 题型加权先于关键词
  3. **语法关键词不全**（连词/虚拟语气/最高级/情态/复数/谓语等靠默认值碰对）→ 关键词表补齐
- 回测：16 条未作答正确跳过；剩余 67 条分布与题型结构吻合（grammar 14 / vocab 21 / discourse 32）
- **待办**：新诊断跑完后人工核对 LLM 直接输出的 error_cause（校准 prompt 规则 7 的实际分类质量）

### 测试
- tests/test_cause_chain.py：26 个测试（归一化/未作答/题型加权、统计兜底、因果链分析、db 存取、报告板块、方案重排、跨周叙事、历史存取）
- 完整套件：89 passed（含既有链路测试，无回归）
