# 拾阶而上 — 操作指南

本指南面向管理员/运营老师（你本人），介绍系统日常操作流程。

> **v2.0（2026-08-04）更新说明：** 核心链路已收敛为家长驱动闭环——家长拍照自助完成诊断/练习/报告；运营端三按钮对应流水线三个恢复点；审核闸门已移除，质量由「抽检 + 纠错」回路保障；B 端（学校/班级/教师）功能已封存（feature flag 默认关闭）。

---

## 一、启动前准备

### 1. 配置环境变量

复制示例文件并填入真实 API key：

```powershell
copy .env.example .env
# 用文本编辑器编辑 .env
```

最小必填项（以 Kimi 为例）：

```text
ANTHROPIC_BASE_URL=https://api.kimi.com/coding/
ANTHROPIC_API_KEY=sk-...
LLM_MODEL=kimi-k2.6
VISION_MODEL=kimi-k2.6
OCR_BACKEND=auto
FLASK_SECRET_KEY=随机字符串
```

### 2. 安装依赖

```powershell
pip install -r requirements.txt
```

### 3. 创建管理员账号

```powershell
python app.py create-admin admin 你的密码 admin
```

### 4. 启动服务

```powershell
python app.py
```

访问 http://127.0.0.1:5000 登录运营后台。

---

## 二、核心链路全景

```
家长（或运营代传）拍照上传试卷
        ↓  【统一额度闸门】订阅 active → 额度充足 → 扣减
        ↓
  批改试卷（grade_only，一次任务跑完整条链）：
        OCR → 错题分析（受控错因归因）→ 错题入库 → 薄弱矩阵
        → 错因因果链画像 → 方案更新（根因优先）
        → 分析报告（含错因画像）→ 针对性练习题（每错题 2 题，无上限）
        ↓
  家长在学习中心看到：报告（错因画像/卡点变化）/ 在线练习（即时判对错）/ 错题本更新
        ↓
  学生练习 → 连对 2 次 = 已掌握 → 错题本越读越薄
        ↓
  周六：自动生成家长周报（含"卡点变化"跨周叙事，本周有分析数据才出）
  周一：自动生成上周错题本快照
  每月 1 日：自动生成月度总结
```

- 任务状态由 **Cycle 状态机** 驱动：`已收卷 → OCR完成 → 错题入库 → 方案更新 → 报告就绪 → 练习就绪 → 周报已发`。
- 任务失败自动**退还额度**，家长可免费重传。
- **错因因果链（2026-08-05）**：每道错题归因到受控五类（单词不认识/语法没掌握/长句拆不开/读不懂文章逻辑/看题不仔细），聚合出核心卡点与传导链；报告与周报按"先补根因"给建议——家长看到的不再是"正确率 60%"，而是"孩子卡在哪、这周练什么、怎么夸他"。

---

## 三、家长自助使用（无需运营介入）

### 0. 学生注册（学生端自助）

学生通过 `/register` 自助注册：姓名、手机号、密码，**必选年级（初一~高三）与教材版本（人教版/外研社版/北师大版/暂不确定）**。

- 教材版本用于后续诊断定位到课本单元（如"卡在人教八下 Unit 5 现在完成时"）
- 填写老师提供的班级码时，年级以班级为准（学校场景一致性）
- 注册后自动进入学习中心 `/s/<access_code>`

### 1. 学情体检（首访）

家长打开 `/parent`：

1. 首次访问 → 拍照上传孩子试卷 → 系统自动建档（体验套餐，1 次免费额度）→ 入学诊断。
2. 诊断完成后获得专属链接 `/s/<access_code>`（保存在浏览器）。
3. 回访自动进入 `/s/<access_code>` 学习中心。

### 2. 学习中心（回访）

`/s/<access_code>` 免登录，包含 9 个 tab：

| Tab | 内容 |
|-----|------|
| 首页 | 学情总览、上传入口 |
| 练习 | 在线交互练习：点选作答 → 即时对错反馈 |
| 报告 | 已生成报告（诊断/分析/批改反馈/周报/月度） |
| 时间轴 | 学习事件流 |
| 成长记录 | 错题本可视化（🔴🟡🟢 + 越读越薄进度） |
| 成就墙 / 复盘 / 坚持日记 | 激励与反思 |
| 进度 | 分数趋势、知识点分布 |

> 家长拍照后系统全自动处理，运营无需做任何事。只有在需要**运营协助建档**（如家长没有手机）或**批量导入**时才走后台。

### 3. 报告与周报怎么解读（给家长答疑用）

- **诊断报告「🔍 错因画像」**：核心卡点（单词不认识 / 语法没掌握 / 长句拆不开 / 读不懂文章逻辑 / 看题不仔细）+ 家长一句话 + 因果链 + 本周聚焦（**先补根因**，次生问题会自己松动）
- **家长周报「🧭 卡点变化」**：对比上周与本周的核心卡点及占比——如"上周的词汇（80%）补上来了，本周新卡点是语法"，这是进步叙事，不是批评
- 家长不需要懂知识点，只需要看懂三件事：**卡在哪、这周练什么、怎么夸他**；家长有疑问时，运营按报告原话解释即可，不要自己发挥诊断结论

---

## 四、运营后台：三个流水线按钮

> 三按钮 = 同一条流水线的三个恢复点（`/api/pipeline/run` 的 stage 参数）。

### ① 批改试卷（grade_only）—— 消耗 1 次额度

1. 「周度服务」页选择学生 → 上传本周试卷照片 → 点「批改试卷」。
2. 系统一次任务跑完整条链：OCR → 错题分析 → 错题入库 → 薄弱矩阵 → 方案更新 → 分析报告 → 练习题。
3. 完成后家长端立即可见（**无审核等待**）。

### ② 矩阵分析（analysis_only）—— 免费

- 无需上传文件，基于已有错题重新生成薄弱矩阵 + 方案增量更新 + 分析报告 + 练习题。
- 用于：家长已上传但想重新分析、或补充分析。

### ③ 生成周报（report_only）—— 免费

- 手动补发家长周报（周六调度器会自动出，此按钮用于补发/重发）。
- 周六自动出报的条件：本周有分析数据（否则不出，避免空报）。

### 推荐节奏

```
每周六：家长/运营上传本周试卷 → 批改试卷（自动完成 ①②）
每周日：检查周报（周六已自动生成，无需手动）
周一：系统自动出上周错题本
```

---

## 五、质量回路（替代原审核闸门）

AI 结果直通家长，质量由两条回路保障：

### 1. 随机抽检（概览页 → 质量）

- 每次任务完成后，系统随机抽 2 条产出进入抽检队列（`/api/safety-checks`）。
- 在「概览 → 质量」查看待抽检项，逐条过审。

### 2. 逐条纠错（周度服务 → 历史任务 → 操作列 → 纠错）

- 对错题/题目/批改结果逐字段纠错。
- 纠错会沉淀为模式（`ai_feedback_patterns`），**自动注入后续 AI 生成的提示词**——纠得越多，AI 越准。

> 运营老师日常工作面 = 抽检队列 + 逐条纠错，而非全量审核。

---

## 六、链路状态与卡住告警（概览页）

- 「概览」页本周状态表新增 **链路状态** 列：显示每名学生当前周期状态（未开始/已收卷/OCR完成/错题入库/方案更新/报告就绪/练习就绪/周报已发）。
- 中间态停留超过 **48 小时** 标红「⚠ 卡住」——通常是任务失败或 worker 异常，去「系统监控 → 最近失败任务」排查后重新触发。

---

## 七、订阅与额度

### 套餐（额度制，按学生）

| 套餐 | 价格 | 额度 | 说明 |
|------|------|------|------|
| 体验 trial | ¥0 | 3 次（一次性） | 注册赠送，用完即止，不按月重置 |
| 包月 monthly | ¥39/月 | 40 次/月 | 自然月重置，当月未用完月底清零 |
| 包年 yearly | ¥399/年 | 600 次/年 | 订阅期内有效，续费换新池 |
| 超级账号 unlimited | ¥0 | 不限 | 自营专用（CLI：`python app.py set-super <access_code>`） |

- 仅「批改试卷」消耗额度；矩阵分析/周报免费；运营账号豁免。
- 收款：线下/私域收款后，在「学生页 → 订阅」选择包月/包月登记收款，系统自动升级套餐并延长有效期（包月 +30 天、包年 +365 天）。

### 额度规则

- 月额度按自然月重置。
- 任务失败自动退额度（家长可免费重传）。

---

## 八、数据合规

### 家长授权

- 运营建档时勾选「家长已同意收集和使用学生学习数据」。
- 未授权学生在「概览」和「合规」页高亮提醒；「合规 → 待家长授权学生」补录。

### 数据删除

- 家长端：公开页底部「申请删除孩子学习数据」。
- 运营端：「合规 → 待处理数据删除申请」→ 执行删除（软删除，公开页失效）。

---

## 九、日常管理与监控

- **学情看板**：学生错题本、分数趋势、薄弱知识点（学习页）。
- **题库管理**：AI 生成的题自动入库，「题库」页可启停/编辑解析。
- **成本监控**：概览页显示本月总成本/单人成本；设置「月度总预算/单人预算」，超限熔断并告警。
- **系统监控**：任务失败率、最近失败任务、告警、审计日志、备份历史。

---

## 十、自动化调度（无需人工）

| 触发 | 任务 |
|------|------|
| 每周六 08:00 后 | 条件式自动周报（本周有分析数据的学生） |
| 每周一 08:00 后 | 上周错题本快照 |
| 每月 1 日 08:00 后 | 上月月度总结 |
| 每天 03:00 | 数据库自动备份（周一加周备份） |

---

## 十一、常见问题

| 问题 | 解决方法 |
|------|----------|
| 启动提示端口被占 | 杀掉残留 python 进程后重试 |
| OCR 识别为空 | 检查 `tessdata/` 是否有语言包；确保图片清晰 |
| 任务一直 processing / 状态卡住 | 「系统监控 → 最近失败任务」排查；卡住的中间态会标红提醒 |
| 家长打不开公开页 | 确认学生未删除；直接用 `/s/<code>` 访问 |
| 家长上传被拒「额度已用完」 | 学生页 → 订阅 → 升级套餐或手动续费 |
| 周报没出 | 周六自动出报需「本周有分析数据」；无数据不出属正常，可手动「生成周报」补发 |
| 想切换 OCR 后端 | 修改 `.env` 中 `OCR_BACKEND`（auto/vision/tesseract） |
| 学校/班级功能入口不见了 | B 端功能已封存（feature flag 默认关闭），恢复方法见 `FEATURE_FLAGS.md` |

---

## 十二、命令速查

```powershell
# 启动服务
python app.py

# 创建管理员账号
python app.py create-admin admin 密码 admin

# 查看账号
python app.py list-admins

# 重置密码
python app.py reset-password 用户名 新密码

# 运行测试（55 项）
python -m pytest tests/ -v
```

---

详细技术文档见 [`DEVELOPMENT.md`](DEVELOPMENT.md)；架构决策见 [`核心链路架构设计.md`](核心链路架构设计.md)。
