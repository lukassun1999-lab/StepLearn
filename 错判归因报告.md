# 判卷误判 · 归因分桶报告（第一轮）

> 日期：2026-08-31 ｜ 工具：`scripts/grading_bucket_analysis.py`（只读）｜ 样本：生产库 180 条真实错题（15 名学生）
> 背景：判卷准确率瓶颈（MiniMax-M3 约 60%）。老师纠错表 `ai_corrections` 暂无真实使用数据（0 条），无法从人工标签分桶；本轮改用**可自动检测的确定性信号**分桶，并导出人工复核表补齐剩余桶。

## 一、分桶方法

对全部存量错题按确定性规则分桶（规则即优先级）：

| 桶 | 判定规则 | 含义 |
|---|---|---|
| B1 归一化命中 | `is_answer_correct(user, correct)` 为真 | LLM 判错但归一化比对命中（大小写/全角/标点/备选答案）——**必为误判** |
| B2 字母前缀命中 | 学生答单字母，标准答案以同一字母+分隔符开头 | 学生选对字母选项（如 user=`e` vs correct=`e. dealing with...`）——高度疑似误判 |
| B3 未作答污染 | 解析/作答含未作答·模糊标记 | 缺做不是错，不应进错题本 |
| B4 格式风险 | 一侧带字母前缀另一侧不带 | 无法自动判定，需人工复核 |
| 覆盖率 | OCR 学生作答题号 vs 落库错题数 | 漏判/误判的代理指标 |

## 二、分桶结果

| 桶 | 条数 | 占比 |
|---|---|---|
| B3 未作答污染 | 48 | **27%** |
| B4 格式风险（待人工复核） | 17 | 9% |
| B1 归一化命中 | 2 | 1% |
| B2 字母前缀命中 | 2 | 1% |
| 未见异常 | 111 | 62% |

任务级覆盖率：task 213（59 题作答 → 29 条错题，49%）、task 215（55 → 22，40%）——单卷错题占比偏高，与家长"批改不准"的体感一致。

## 三、关键发现：问题集中在存量

按提交时间切分（08-28 `67a0d82`/`338ae7f` 强化批改过滤、08-29 `6e93033` 答题卡双输入）：

- **B1/B2/B3/B4 共 69 条全部是修复提交之前的存量，新数据 0 条命中。**
- 结论：现行 prompt 规则 + `_filter_real_mistakes` 后处理对**新数据已基本生效**；60% 准确率的体感主要由两类构成：
  1. **存量污染**（27% 未作答 + 4% 误判）今天仍在错题本/错因画像/周报里发酵；
  2. 剩余为"自定答案错"（无标准答案时模型解错题）——本轮无法自动测量，待人工复核。

## 四、决策（对照预定路径）

| 原计划 | 决策 | 依据 |
|---|---|---|
| 归因分桶 | ✅ 完成（脚本可反复跑） | 本报告 |
| ④ temperature 0 | ✅ 实施 | 判卷是确定性任务，成本为零 |
| ① 印刷答案直读 | ✅ 实施 | 剩余不可测桶（自定答案错）的最大确定性解法 |
| ② 三步拆解 + 代码比对 | ⤵️ **简化为后处理增强**：B2 字母前缀规则 + `answered_count` 覆盖度遥测 | 代码比对后处理已存在且对新数据生效；完整三调用重构风险大于收益 |
| ②' 存量清洗 | ✅ 新增（分桶催生） | 69 条存量违反现行产品规则，仍污染周报与错因画像 |
| ③ 多票自洽 | ⏸ **暂缓** | 可测桶中代码可修项占大头，"自定答案错"占比未知；符合"占比不高就省下"的预定条件 |

## 五、后续

1. **人工复核**（需运营执行）：`错判人工复核表.csv` 69 条中 B4（17 条）逐条确认；另从未见异常桶抽 20 条核对"自定答案对不对"→ 得出"自定答案错"真实占比，再决定是否重启 ③
2. 存量清洗脚本默认 dry-run，确认无误后 `--apply`
3. 每次批量纠错后重跑 `python scripts/grading_bucket_analysis.py`，跟踪桶分布变化
