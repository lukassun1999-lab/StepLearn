# 错因因果链 · 运营校准报告（第一轮）

> 日期：2026-08-05 ｜ 校准人：创始人（运营老师角色）＋ AI 审查 ｜ 样本：生产库 83 条真实错题

## 一、校准方法

1. 对生产库（data.db）执行错因因果链最小迁移（mistakes 加列 + cause_profiles 建表）
2. 导出全部 83 条存量真实错题（题干 / 题型 / 解析）
3. 用 `_normalize_error_cause` 兜底映射逐条审查，找出系统性偏差
4. 修正映射逻辑 → 回测对比

> 说明：存量错题为旧 prompt（无受控错因）生成，校准的是**兜底映射**（LLM 漏输出时保证分类质量）；新 prompt 已要求 LLM 直接输出受控 error_cause，质量只会更高。

## 二、发现的问题（三个系统性偏差）

### 1. 未作答/缺做题污染错因统计（最严重）
- 83 条中约 16-20 条是"学生未作答/空白"（如 [63]-[75] 阅读表达题），没有认知层面的错因
- 旧逻辑把它们按关键词/默认归入 grammar 或 discourse——**完全错误**，会扭曲错因画像（把"没做题"误诊为"语法弱"）

### 2. 题型特征未利用，导致错因误归
- **完形填空**的词义辨析（[21][25][26][28]）被"上下文"关键词误归 discourse，实际是**词汇量/词义**问题 → 应归 vocab
- **补全对话/情景交际**（[33]-[37]）被默认归 grammar，实际是**日常表达积累** → 应归 vocab
- **阅读理解/匹配**本身即语篇问题，旧逻辑靠"主旨/推理"关键词碰对，不稳

### 3. 常见语法词不在关键词表（靠默认值碰对，脆弱）
- "连词""虚拟语气""最高级/比较级""情态动词""复数""谓语"等常见语法错误表述未收录 → 全部落到默认 grammar，碰对但不可控

## 三、修正内容（skills_bridge.py）

1. **`_is_unanswered()`**：按解析文本标记识别未作答（"未作答/空白/未填写…"），返回 None 不进统计；user_answer 缺失不判未作答（避免误伤 LLM 漏输出）
2. **题型加权**（先于关键词）：
   - 阅读/匹配/任务型/信息 → discourse
   - 补全对话/情景/交际 → vocab
   - 完形填空 → 默认 vocab（词义选择），明确涉及语法规则才归 grammar
3. **关键词补齐**：vocab 加"辨析/固定表达/情景交际"；grammar 加"连词/虚拟/最高级/比较级/情态/复数/谓语/代词/成分"
4. **prompt 规则 6 补充**："学生未作答/空白的题不要放进 mistakes（缺做不是错，无错因可归）"

## 四、回测结果（83 条真实样本）

| 项 | 修正前 | 修正后 |
|---|---|---|
| 未作答被误归错因 | ~20 条污染 | **16 条正确跳过（19%）** |
| 有效错因样本 | 63 条（含污染） | 67 条（不含未作答） |
| 分布 | grammar 被污染偏高 | grammar 14 / vocab 21 / discourse 32，与题型结构吻合（阅读类占比最高） |

样本中无长难句（syntax）与粗心（careless）类题目，属样本覆盖限制，不代表映射失效。

## 五、持续校准建议

1. **新诊断校准（待你执行）**：下一次真实诊断跑完后，对照"错因画像"板块人工核对 10 条错题的 error_cause 与 cause_evidence，错分超过 2 成则调整 prompt 五类定义示例
2. **关注 LLM 直接输出的分类质量**：兜底映射已校准，接下来主要校准 LLM 输出（新 prompt 规则 7）
3. **syntax/careless 样本积累**：当前样本缺这两类，遇到长难句/粗心类错题时重点核对
4. **校准样本资产化**：83 条真实错题可沉淀为回归样本集（去隐私化后），防止未来映射回归
