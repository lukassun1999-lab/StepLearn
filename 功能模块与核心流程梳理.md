# 拾阶而上（StepLearn）— 功能模块与核心流程深度梳理

> 梳理日期：2026-08-04 首版 · **2026-08-21 按代码现状全面更新**（app.py ~350 行装配层 / db/ 包 36 张表 / 122 条路由 / 289 测试用例）
> 2026-08-04 首版后发生的主要演进：P1-P3 链路收敛（Cycle 状态机 + 声明式流水线 + app.py 拆蓝图）、P4 错因因果链、新计价体系、安全止血与合规硬删、skills_bridge 拆 7 模块、db.py 拆 db/ 包、微信场景 UI 重构。`README.md` / `DEVELOPMENT.md` 已同步重写，与本文一致。

---

## 一、项目定位与演进

### 1.1 产品定位（已转型）

| 维度 | 说明 |
|------|------|
| 产品名 | 拾阶而上 / StepLearn（原「周末英语托管管理系统」） |
| 核心价值 | **家长拍照 → AI 自动分析 → 家长看到孩子进步、肯定成长 → 孩子拿到针对性练习、减少盲目刷题 → 错题本越薄越有成就感** |
| 产品性质 | **家长驱动的亲子学习可见性工具**（C 端），不是老师管理系统 |
| 目标用户 | 高中生家庭；付费方为家长 |
| 架构模式 | 单租户自营（非多租户 SaaS，无 tenant_id） |
| 当前形态 | C 端最小闭环模式：学校/教师功能由 feature flag 关闭（默认 OFF） |

### 1.2 角色体系

| 角色 | 入口 | 凭证 | 说明 |
|------|------|------|------|
| 管理员 | `/login` | 账号密码（admin_users.role=admin） | 全权限：合规执行、账号管理、预算 |
| 老师（运营者本人） | `/login` | 账号密码（role=teacher） | 上传/抽检/纠错；额度豁免 |
| 学生 | `/student-login` / `/register` → `/s/<access_code>` | 手机号+密码 | 注册必选年级+教材版本；登录后统一进学习中心（原 `/student` 页已删除） |
| 家长 | `/s/<access_code>` 或 `/parent` | **无需账号**，凭学生 access_code 链接 | 上传试卷、看报告、陪学任务、复盘 |
| 教师/机构（B 端，已停用） | `/teacher-register` | feature flag 关闭，路由 404 | 见 FEATURE_FLAGS.md |

### 1.3 技术栈

| 层 | 选型 |
|----|------|
| 后端 | Python 3 + Flask；`app.py` 仅装配层（~350 行），路由拆入 `web/` `api/` `b_end/` 蓝图；生产入口 `wsgi.py` |
| 数据库 | SQLite（WAL + busy_timeout=15000），单文件 `data.db`，36 张表；`db/` 包唯一数据源 |
| 异步任务 | `threading` + `queue.Queue`，3 worker 线程池（PIPELINE_WORKERS）+ 按学生串行锁 |
| LLM | Anthropic / OpenAI 兼容接口双后端（当前接 Kimi/MiniMax），无 key 自动 demo 模式（显式告警） |
| OCR | 多模态 vision LLM 为主（专用手写试卷识别 prompt），Tesseract.js 自动降级 |
| 前端 | 原生 HTML/CSS/JS 内联（`web/templates_*.py`，无构建步骤）；Notion 风暖色设计系统（DESIGN.md，accent `#e07b4b`） |
| 日志 | 标准库 logging（stderr + logs/app.log 轮转，LOG_LEVEL 可调） |
| 短信 | `sms.py` 为 mock（打印到控制台，含防爆破锁定），未接真实短信服务商 |

---

## 二、系统架构

```
┌────────────────────────────────────────────────────────────┐
│ app.py — 装配层（~350 行）：Flask 实例 + 蓝图注册 + CLI       │
│  web/pages.py        页面（/ · /login · /s/<code> · /parent）│
│  api/family_api.py   家庭端 API（27 条）                     │
│  api/ops_api.py      运营端 API（70 条）                     │
│  b_end/routes.py     B 端封存（18 条，flag 守卫）             │
└──────────────────────────┬─────────────────────────────────┘
                           │ enqueue_task()
┌──────────────────────────▼─────────────────────────────────┐
│ pipeline_worker.py — 任务队列 + 3 worker 线程               │
│  · 注册表模式分发 task_type → handler                        │
│  · 按学生锁串行，防同学生并发竞态                             │
│  · 完成后随机抽检入 aigc_safety_checks                       │
│  · grade_only 一次任务跑完整条链（ocr→exercises）            │
│  · 失败 → 记录错误 + 退还额度（quota_charged）               │
│  · 启动时回收僵尸任务（<24h 自动复活，最多 2 次；否则退额度） │
└──────────────────────────┬─────────────────────────────────┘
┌──────────────────────────▼─────────────────────────────────┐
│ pipeline/ — 声明式流水线                                     │
│  engine.py（链执行器：恢复点 + 断点续跑）                    │
│  stages.py（ocr/analyze/plan/analysis_report/exercises/     │
│             weekly_report）· snapshots.py（批改/周错题本/月度）│
│  cycle_pipeline.py（onboarding+weekly 统一 handler）         │
│  scheduler.py（周六条件周报/周一错题本/月度总结）             │
└──────────┬─────────────────────────┬───────────────────────┘
┌──────────▼───────────┐   ┌─────────▼──────────────┐
│ domain/              │   │ skills_bridge（门面）    │
│  cycle 状态机         │   │  → 7 模块：ocr_service/ │
│  upload/quota 闸门   │   │    llm_analysis/        │
│  questions 取题      │   │    question_gen/        │
│  grading 判分        │   │    llm_plans/           │
└──────────────────────┘   │    knowledge_base/      │
                           │    llm_prompts/         │
   ┌────────────────────┐  │    bridge_common        │
   │ report_templates.py│  └────────────┬───────────┘
   │ 6 类 HTML/PDF 报告  │               │
   └────────────────────┘  ┌─────────────▼──────────┐
                           │ llm.py · db/（36 表）   │
                           └────────────────────────┘

辅助：backup.py（03:00 自动备份）· sms.py（mock 验证码）· log_setup.py
     一次性数据修复脚本已归档 scripts/archive/
```

---

## 三、功能模块全景

### 模块 1：认证与用户体系
- 管理员/老师：账号密码登录（werkzeug 哈希），session 鉴权，`@login_required`、`@admin_required`
- 学生：手机号+密码注册登录；短信验证码登录/重置密码（`/api/sms/*`，sms.py 为 mock）
  - **注册必填年级（初一~高三）与教材版本（人教版/外研社版/北师大版/暂不确定）**（2026-08-05）；填班级码时年级以班级为准
  - 年级/教材版本将用于教材单元映射与诊断定位（"卡在人教八下 Unit 5 现在完成时"）
- 家长：无账号体系，凭 `/s/<access_code>` 链接访问（公开页所有 API 以 code 校验）
- B 端教师注册、学校/班级体系：存在但被 feature flag 关闭（18 个路由受控）

### 模块 2：学生档案与个性化画像
- `students`：基础档案 + access_code / parent_access_code
- `student_profiles`：**40+ 字段六大部分画像**（基本信息/时间全景图/英语学情/深度学习特质/目标与支持/孩子心声），另有词汇量测评结果、关键抉择（plan_choices）、家长任务包进度（parent_task_progress）
- 画像驱动 `LEARNING_PLAN_PROMPT`，生成真正个性化的学习方案（学习风格四维评估、时间效率、心理动机等）
- 入学三步流程嵌入画像采集（onboarding profile tabs：英语学情/学习特质/目标与支持）

### 模块 3：AI Pipeline 引擎（核心中的核心）
- **Cycle 状态机**（domain/cycle.py）：每个「学生 × 周 × 类型（diagnostic/weekly）」是一个周期，状态线性推进 `created → paper_received → ocr_done → graded → analyzed → report_ready → exercised → reported`；物理存储于 weekly_records（kind/stage 列）
- **声明式链**（pipeline/engine.py）：`ocr → analyze → plan → analysis_report → exercises`，weekly_report 独立节点；恢复点 + 断点续跑（僵尸复活按 cycle.stage 从下一节点继续，OCR 文本持久化免重复识别）
- **任务生命周期**：pending → processing → done/failed；`ai_tasks` 记录 input_data/output_data/progress/current_step/error_message（needs_review 列保留但恒 0，审核闸门已移除）
- **onboarding（入学诊断）**：diagnostic 链（ocr → analyze → plan → analysis_report），学习风格回写画像
- **stage 参数 → 链起点映射**：

| stage | 名称 | 输入 | 链执行区间 | 消耗额度 |
|-------|------|------|-----------|---------|
| `full` / onboarding | 完整链 | 试卷照片 | ocr → exercises（diagnostic 链无 exercises） | 是 |
| `grade_only` | 批改试卷 | 试卷照片 | ocr → exercises（**一次任务跑完，不再自动链第二任务**） | 是 |
| `analysis_only` | 矩阵分析 | 无文件 | plan → exercises | 否 |
| `report_only` | 生成周报 | 无文件 | weekly_report 单节点（周六条件自动共用） | 否 |
| `grade_exercises` | 练习批改 | 答案照片 | 独立快照任务：AI 批改→掌握度更新→反馈报告+成绩记录 | 是 |
| `weekly_mistake_book` | 周错题本 | 无（周一自动） | 独立快照：上周错题本快照 HTML | 否 |
| `monthly_summary` | 月度总结 | 无（1 号自动） | 独立快照：AI 月度分析 + 月度报告 HTML | 否 |

### 模块 4：OCR 引擎（skills_bridge）
- `run_ocr`：OCR_BACKEND=auto 时 vision 优先、失败降级 Tesseract.js
- vision OCR 使用**专用手写试卷 prompt**：分「试卷正文」+「学生作答」两部分输出，严格要求照抄印刷题号、处理选项粘连（如 "Bofull of"→"B. full of"）、模糊不猜测标记 `[模糊]`
- `run_ocr_parallel`：多页试卷线程池并行 OCR（最多 4 并发），按页序拼接
- 置信度启发式：`[模糊]` 标记密度换算；OCR 文本不足 50 字时不停止流水线，用占位提示走通用诊断

### 模块 5：错题本与掌握度追踪
- `mistakes`：题干/题型/正确答案/学生答案/解析/知识点(JSON)/难度/mastery_level/**consecutive_correct**/error_cause（受控五类错因）/cause_evidence（判断依据）
- **掌握判定：连续答对 2 次（consecutive_correct >= 2）即掌握**，错题本「越读越薄」
- **错因归因（2026-08-05）**：每道错题归因到受控五类（vocab 词汇 / grammar 语法 / syntax 句法 / discourse 语篇 / careless 审题），未作答/缺做不进错因统计；LLM 输出缺失时按题型加权+关键词兜底映射
- 错题来源：入学诊断、每周试卷、练习批改；`practice_records` 记录每次作答
- 答案归一化防误判：`_filter_real_mistakes` 剔除「答对却被判错」的条目（去选项字母前缀、忽略大小写/引号/空白）
- 类似题生成：`generate_similar_questions` 按单道错题生成同知识点变式题（三重相似度去重：对原题 >0.7、对已有题 >0.65 即丢弃）
- 错题有间隔复习字段（next_review_at/review_interval_hours/review_stage，艾宾浩斯式）

### 模块 6：题库系统
- `questions` 表沉淀 AI 生成题，带 `source_mistake_id` 溯源、usage_count、enabled 开关
- **出题策略：题库复用优先**（按知识点 find_similar_questions），不足部分 LLM 生成补齐，新题入库供未来复用
- **题量策略（2026-08-04 调整）：每错题 2 道同一知识点练习题，不设总量上限**（原 15 道封顶已取消）
- 主观题型（任务型阅读/阅读理解/写作/书面表达）不进逐题练习（无标准判分）
- 管理端题库 CRUD + 统计（/api/questions/*）

### 模块 7：报告体系（report_templates，服务端渲染 HTML）
| 报告 | 函数 | 说明 |
|------|------|------|
| 诊断/矩阵分析报告 | render_diagnostic_report | 错题明细+薄弱点矩阵+学习方案（含家长成长任务包、激励卡、元认知复盘问题）**+ 错因画像板块**（核心卡点/家长一句话/因果链/根因聚焦） |
| 练习卷 | render_exercise_sheet | 每错题 2 道同类练习（无总量上限） |
| 练习卷 PDF | render_exercise_pdf | 可打印 PDF 下载（/api/public/<code>/exercise-pdf） |
| 批改反馈 | render_feedback_report | 逐题对错+解析+总结 |
| 家长周报 | render_weekly_report | 成长叙事（台阶隐喻）+ 周对比 + 三方行动方案 **+ 卡点变化板块**（错因跨周对比叙事） |
| 周错题本 | render_weekly_mistake_book | 周一自动生成上周错题快照 |
| 月度报告 | render_monthly_report | AI 月度分析（进步/关注/建议） |

所有报告以 HTML 文件存 `uploads/<sid>/<file_type>/`，登记入 `files` 表。

### 模块 8：订阅计费与额度系统
- **定价（PRICING，2026-08-16 新计价体系，db/core.py）**：

| 套餐 | 价格 | 额度 | 说明 |
|------|------|------|------|
| trial 体验 | ¥0 | 3 次（一次性池） | 注册赠送，不按月重置，用完即止 |
| monthly 包月 | ¥39/月 | 40 次/月 | 自然月重置，当月未用完月底清零 |
| yearly 包年 | ¥399/年 | 600 次池 | 订阅期内有效（不按月清零），续费换新池 |
| unlimited 超级账号 | ¥0 | 不限 | 自营专用（CLI：`python app.py set-super <access_code>`） |

- 旧套餐迁移映射：standard/basic → monthly，premium → yearly
- 额度机制：`_ensure_quota_reset` 按 reset_period 重置；仅 OCR 重阶段（链起点为 ocr）消耗，`analysis_only`/`report_only` 免费；老师/管理员豁免
- **失败退额度**：任务失败或僵尸任务被收尸时 `refund_quota`，家长可免费重传
- 付款：线下收款登记（payments）自动联动套餐（包月 +30 天、包年 +365 天）；同套餐包月续费不清当月用量，包年续费换新 600 次池
- **成本护栏**：LLM 每次调用写 `llm_usage_log`（token/成本/耗时/重试/缓存），按学生/月度归集；`check_cost_budget` 预算熔断（月度总预算+单学生预算），超限拒绝新任务并产生 critical 告警

### 模块 9：学生/家长公开页 `/s/<code>`（C 端主阵地，底部 5 导航，2026-08-17 重构）
| 导航 | 功能 |
|-----|------|
| 首页 | 学情总览、最近报告、上传入口（多图选择 + 客户端压缩，微信场景适配） |
| **练习** | **在线交互练习**：取未掌握错题关联题（≤15）→ 点选作答 → 即时对错反馈+解析 → record_practice 更新掌握度（带归属校验，题库复用题不污染他人数据） |
| 报告 | 报告列表+内联预览/下载（完成提示可直达报告，空壳报告自动过滤） |
| 成长 | 子页：时间轴（学习事件流）/ 成就墙（achievements 徽章）/ 成长记录（错题本可视化：按知识点分组、🔴🟡🟢 三级、「越读越薄」进度） |
| 我的 | 子页：复盘（元认知双视角问答）/ 坚持日记（打卡日历）/ 学习进度（分数趋势、知识点分布）；底部数据删除申请入口 |

家长公开页专属能力：拍照上传试卷（多图，自动触发流水线+轮询真实进度）、提交数据删除申请、邀请有礼入口、成长海报保存。

### 模块 10：家长 H5 学情体检 `/parent`
- 冷启动获客入口：家长首次拍照 → **自动创建学生**（"家长用户_xxx" + access_code）→ onboarding 诊断 → 三步进度 UI（上传→分析→报告）→ 留存 access_code 于 localStorage
- 回访家长凭已有 code 复用学生档案，走 weekly 流水线

### 模块 11：AI 安全与质量闭环
- **抽检**：任务完成后随机抽 2 条产出（错题快照或批改记录）入 `aigc_safety_checks`，老师人工过审
- **纠错回流**：老师可对错题/题目/批改结果逐字段纠错（`ai_corrections`），纠错沉淀为模式（`ai_feedback_patterns`），**下次相关 prompt 自动注入纠错提示**（get_recent_correction_hints）
- 纠错可撤销、有统计看板

### 模块 12：审核流（P3 已移除）
- **审核闸门已删除**（P3/D1 决策）：AI 结果直通家长，无需人工审核
- approve/reject/批量审核端点与前端审核队列已删除；`needs_review` 字段保留但恒 0
- 质量控制全部由模块 11 的质量回路承担（抽检 + 纠错回流）
- 纠错入口位于「周度服务 → 历史任务 → 操作列 → 纠错」

### 模块 13：增长与留存
- 转介绍：邀请码（referrals）+ 成果海报（/api/poster/<code>）+ 邀请统计
- 打卡 + 成就墙 + 周报成长叙事 = 留存三件套

### 模块 14：合规（未成年人数据）
- 家长授权记录（parent_consents）：版本化 + 支持撤回；未授权学生在合规页/仪表盘提醒；CONSENT_REQUIRED=true 时无同意禁止上传
- 数据删除申请：老师或家长发起 → 管理员执行**注销级联硬删**（PIPL）：清除学生档案及其错题/任务/文件/画像等全部个人数据，仅匿名保留支付对账与审计记录，不可恢复
- 全部写入 audit_logs

### 模块 15：错因因果链诊断（2026-08-05，差异化核心）
- **目标**：诊断从"哪里不会"（知识点统计）升级为"为什么不会、先补什么"（因果链画像）——竞品知识图谱是横断面，我们是因果链
- **受控五类错因**（vocab/grammar/syntax/discourse/careless）：分析 prompt 规则 7 受控输出 + `_normalize_error_cause` 兜底（题型加权+关键词映射），未作答自动跳过
- **因果链画像**（cause_profiles 表）：核心瓶颈（primary_cause）+ 传导链（cause_chain）+ 根因优先聚焦知识点（priority_kps）+ 家长一句话（plain_language，模板填空不走 LLM 自由发挥）；LLM 不可用时 `_statistical_cause_profile` 统计兜底
- **方案根因优先**：LEARNING_PLAN_PROMPT 引用画像 + `_reorder_priority_by_cause` 确定性重排（根因排最前、severity 升"高"）
- **跨周叙事**（cause_profile_history 表）：`build_cause_trend` 模板生成"卡点变化"（缓解/加剧/迁移），周报展示——家长连续 3-4 周可看到"词汇 80% → 50% → 新卡点变语法"的成长轨迹
- **内容管控**：LLM 只做归因与判断（诊断类），家长话术全部模板化，遵守 AI 内容管控原则
- 运营校准：83 条真实错题回测修正（未作答污染/题型加权/关键词补齐），详见《错因校准报告.md》
- 实施与设计详见《错因因果链实施方案.md》

### 模块 16：可观测性与运维
- 任务失败率统计、最近失败任务（含 traceback 前 5 帧）、系统监控页
- 告警中心（alerts：成本/任务）、可忽略、阈值可配置
- 审计日志（筛选 + 动作枚举）
- **自动备份**：backup.py 每天 03:00 日备 + 每周一周备（sqlite3.backup 在线一致性备份），自动清理，可手动触发/下载
- 僵尸任务自愈：服务重启后 pending 重新入队；processing 僵尸 <24h 自动复活（最多 2 次），超期标记失败并退额度

### 模块 17：Feature Flags（C 端模式开关）
- `feature_school_enabled` / `feature_teacher_enabled` 默认 OFF（凯哥建议：先做学生/家长最小闭环）
- 存储于 settings 表，后端 `@feature_required` 守卫（关闭时 404）+ 前端 Jinja 条件渲染
- 控制 18 个路由：学校/班级 CRUD、班级码、教师注册与档案等；恢复命令见 FEATURE_FLAGS.md

---

## 四、核心流程详解

### 流程 1：家长核心价值链（产品的生命线）⭐

```
家长拍照上传试卷
  │  /api/public/<code>/upload（或 /api/parent/diagnose 首次）
  ▼
[闸门] 订阅 active? → 额度剩余? → consume_quota（记 quota_charged）
  ▼
create_task(weekly, stage=grade_only) → enqueue
  ▼ worker（按学生锁串行，同一任务跑完整条声明式链）
ocr        OCR 识别试卷（vision LLM，多页并行，长卷按题型分节）
  ▼
analyze    AI 分析错题（只提取真错题 + 答案归一化兜底 + 受控五类错因归因）
  ▼        错题入错题本 + practice_session（未作答不进 mistakes）
plan       薄弱矩阵 + 错因因果链画像（cause_profiles）
  ▼        学习方案：已有 → generate_plan_update 增量（根因优先重排）；无 → 全新生成
analysis_report  分析报告 HTML（含错因画像板块）
  ▼
exercises  每错题 2 道同知识点练习（题库复用优先，LLM 补齐，入库沉淀，无总量上限）
  ▼
家长轮询 /api/public/<code>/task/<id> → 完成后直达报告（内联预览）
  ▼
公开页呈现：报告 + 练习 tab 可在线做题 + 错题本更新
```
> 关键设计：家长只做一个动作（拍照），后续全自动；一次任务跑完，失败自动退额度可重试。

### 流程 2：入学诊断（onboarding，新家长首次）

```
/parent 页拍照上传 → 无 code → 创建学生「家长用户_xxx」+ access_code
→ onboarding pipeline：
  OCR(并行) → 分析错题 → 存错题/会话 → 薄弱点矩阵 → 生成学习方案
  → AI 评估的学习风格回写画像 → 诊断报告 HTML → 更新周记录
→ 返回 access_code，家长 localStorage 保存，此后进入回访链路
```

### 流程 3：周度三按钮（= 同一条声明式链的三个恢复点，可独立执行）

```
① 批改试卷 grade_only：ocr 起点，一次任务跑完 ocr→…→exercises（消耗额度）
② 矩阵分析 analysis_only：plan 起点，薄弱矩阵 → 方案增量更新 → 分析报告 → 练习题（免费）
③ 生成周报 report_only：weekly_report 独立节点，周统计 + 跨周对比 → 家长周报（免费；周六条件自动共用）
推荐顺序：①（已含②的分析段）；② 用于无新卷时的重新分析；③ 用于补发
```
方案自适应规则（PLAN_UPDATE_PROMPT 硬约束）：
- 综合完成率（学生 60% + 家长 40% 权重）≥80% → 下周难度 +10%
- 50%~80% → 维持，优化摩擦；家长参与低则鼓励参与
- <50% → 难度 -20% + 防放弃设计，区分学生端/家长端掉队

### 流程 4：练习闭环（掌握度收敛）

```
A. 在线做题（公开页练习 tab）：
   取未掌握错题的关联题 → 学生点选 → 即时判对错 + 解析
   → record_practice（仅当题目 source_mistake 属于该学生）
   → 连对 2 次 → 错题标记掌握 → 错题本变薄 + 成就/进度可视化

B. 纸面批改（grade_exercises stage）：
   答案照片 → OCR → 加载该生本周练习题（含题库回退，回退题切断 source 关联防污染）
   → AI 批改 → record_practice 更新掌握度 → 反馈报告 + 正确率记入 score_history
```

### 流程 5：自动化调度（无人值守）

| 触发 | 任务 | 说明 |
|------|------|------|
| 每周六 08:00 后 | weekly_report（条件式） | 本周有分析数据才出，避免空报（DB 级当日去重） |
| 每周一 08:00 后 | weekly_mistake_book ×全体活跃学生 | 上周错题本快照（DB 级当日去重防重启重复） |
| 每月 1 日 08:00 后 | monthly_summary ×全体活跃学生 | 上月 AI 月度分析报告 |
| 每天 03:00 | 日备份（周一加周备份） | sqlite3.backup 在线备份 + 自动清理 |
| 服务启动 | 任务恢复 | pending 重入队；僵尸任务复活/收尸+退额度；定时任务窗口补跑 |

### 流程 6：质量保障回路（替代已删除的审核闸门）

```
任务完成 → 随机抽检 2 条入 aigc_safety_checks → 运营逐条过审
老师纠错（周度服务 → 历史任务 → 纠错）→ ai_corrections 落库
      → ai_feedback_patterns 沉淀 → 下次 analyze/generate/grade prompt 自动注入「历史纠错提示」
```

---

## 五、数据模型（36 张表，按域分组）

| 域 | 表 |
|----|----|
| **核心实体** | students（档案+年级+教材版本+access_code）、student_profiles（40+字段画像） |
| **教学** | mistakes（错题+受控错因）、practice_records（作答）、practice_sessions（考试会话）、learning_plans（方案+薄弱矩阵）、plan_updates（方案更新史）、questions（题库）、score_history（分数趋势）、cause_profiles（错因因果链画像）、cause_profile_history（画像周历史，跨周对比）、unmapped_kps（未识别知识点标签池，数据驱动补词） |
| **流程** | ai_tasks（任务状态机+cycle_id）、files（文件元数据）、weekly_records（**Cycle 表**：(student_id, week_start, kind) 唯一，stage 状态机 + 兼容布尔标记） |
| **商业** | subscriptions（订阅+额度）、payments（付款流水）、referrals（转介绍） |
| **成长激励** | check_ins（打卡）、achievements（成就墙）、metacognitive_reviews（元认知复盘） |
| **成本** | llm_usage_log（每次 LLM 调用 token/成本） |
| **AI 安全** | aigc_safety_checks（抽检）、ai_corrections（纠错）、ai_feedback_patterns（模式沉淀） |
| **合规** | parent_consents（家长授权，版本化+撤回）、deletion_requests（删除申请） |
| **可观测** | alerts（告警）、audit_logs（审计）、backups（备份记录） |
| **账号** | admin_users（含 role/phone/subject/school_id）、sms_codes（验证码） |
| **B 端（停用）** | schools、classes、teacher_profile |
| **系统** | settings（KV：预算/feature flags 等） |

关键关系：`students` 1→N 几乎所有业务表；`mistakes` 1→N `practice_records`；`questions.source_mistake_id` → mistakes（掌握度回写链路）；`ai_tasks` 1→N `llm_usage_log`（task_id 归集成本）。

---

## 六、API 概览（122 条路由，按域聚类；审核闸门相关 approve/reject/batch 已于 P3 删除）

| 域 | 代表路由 |
|----|---------|
| 认证 | /login /logout /api/auth/me /api/student-login /api/register /api/sms/* |
| 仪表盘 | /api/dashboard /api/status /api/cost /api/cost/alerts |
| 学生档案 | /api/students CRUD /api/students/<id>/profile(+parent-tasks) |
| 流水线 | /api/pipeline/run /api/tasks[/<id>] /failure-stats /recent-failures |
| 错题与练习 | /api/students/<id>/mistakes /due-reviews /api/mistakes/<id>/master /similar /batch-similar |
| 打卡成就 | /api/checkin /api/students/<id>/checkins /achievements |
| 公开页 | /api/public/<code> /practice(+submit) /upload /task/<id> /reports /exercise-pdf /review /timeline /achievements /request-deletion |
| 家长端 | /api/parent/diagnose /task/<id> /progress/<code> |
| 题库 | /api/questions CRUD /toggle /stats |
| 学情看板 | /api/learning/class /student/<id> /score |
| 商业 | /api/subscriptions /payments /budget /api/referrals/* /poster/<code> |
| 文件 | /api/upload /api/files/<id>/download |
| 纠错 | /api/tasks/<id>/correctables /corrections /api/corrections/<id>/revert /stats |
| 抽检 | /api/safety-checks/pending /stats /<id>/review |
| 运维 | /api/alerts /audit-logs /backups /api/admin/alert-settings /api/admin/users |
| 合规 | /api/compliance/consents /deletion-requests(+process) /students-without-consent |
| 周度 | /api/weekly |
| B 端（flag 关闭） | /api/schools /api/classes /api/class/* /api/teacher* /api/my-classes |
| 页面 | / /login /student-login /register /s/<code> /parent（/student 页已删除；/teacher-register 随 flag 停用） |

---

## 七、工程现状观察

### 7.1 文档与代码的一致性（2026-08-21 已对齐）
- `README.md` / `DEVELOPMENT.md` / 本文均已按代码现状重写：家长驱动叙事、声明式链、Cycle 状态机、36 表、新计价、硬删合规、模块拆分
- 历史遗留缺口「家长上传只触发 grade_only、练习 tab 会空」→ 已根治：P1 起 grade_only 一次任务跑完整条链（原自动链 hack 已删）

### 7.2 技术债与风险
| 项 | 说明 |
|----|------|
| `llm.py::_default_client` 全局单例 | 多 worker 竞态（历史遗留，文档已记载） |
| sms.py 为 mock | 短信验证码仅打印控制台；真实短信登录/注册上线前必须接真实服务商 |
| 报告为 HTML | 微信内打开体验尚可，PDF 化仅覆盖练习卷 |
| 模板内联 JS 巨大 | templates_admin / templates_family 体量大，前端改动需小心；暂无组件化计划 |

已解决（P2/P3/拆分）：app.py 万行单文件（→ 蓝图 + 装配层）、db.py 单文件（→ db/ 包）、skills_bridge 巨石（→ 7 模块）、`_migrate_db` 重复定义、一次性脚本与根目录杂物（→ archive/、scripts/archive/）。

### 7.3 测试覆盖
`tests/`（**289 用例**，25 个测试文件）：OCR 后端调度与降级、合规流程、流水线并发/僵尸恢复/额度退还、注册流程、错因因果链单元+端到端、知识点词表归一化、判分归一化、新计价、安全防线、时区边界、日志、拆分门面回归、页面冒烟等。运行：`python -m pytest tests/ -v`。

### 7.4 商业化状态
Phase 1-5 + P1-P4 功能全部完成，处于冷启动商业验证期（无付费用户）；B 端功能已封存；系统计价已定版（trial 3 次 / 包月 ¥39 / 包年 ¥399 / 超级账号），当前线下/私域收款，在线支付待接入。
